<div class="sheet">
  <header class="sheet-header">
    <h1>{{ homepage_products.title }}</h1>
      {{ homepage_products.description }}
  </header>
  
  <section class="group products" data-products-size="{{ homepage_products.products.size }}">
    {% for p in homepage_products.products %}
      {% include 'product_item' %}
    {% endfor %}
  </section>
</div>