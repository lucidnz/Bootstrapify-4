var SixPackHolderItemsDisplay = function ($ele, limit_multiple) {
  this.$ele = $ele;
  this.limit_multiple = limit_multiple;
};

SixPackHolderItemsDisplay.prototype.update = function (total_item_count) {
  
};

module.exports = SixPackHolderItemsDisplay;