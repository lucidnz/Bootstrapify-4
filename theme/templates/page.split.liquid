<main role="main">
  <article class="sheet">
    <header class="sheet-header page-header">
      <h1>{{ page.title }}</h1>
    </header>

    <section class="sheet-content">
      {% include 'content_split' with page.content %}
    </section>
  </article>
</main>

{% include 'site_aside' %}