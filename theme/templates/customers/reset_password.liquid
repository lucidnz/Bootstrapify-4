<main role="main">
  <article class="sheet">
    <header class="sheet-header">
      <h1>{{ 'customer_accounts.reset_password.page_title' | t }}</h1>
    </header>

    <section class="sheet-content">
      <div class="customer-register-form">
        <p class="description">{{ 'customer_accounts.reset_password.description' | t: email: email }}</p>
    		{% include 'form_customer_password' with 'reset' %}
      </div>
    </section>
  </article>
</main>

{% include 'site_aside' %}