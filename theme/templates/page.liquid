<main role="main">
  <article class="sheet">
    <header class="sheet-header">
      <h1>{{ page.title }}</h1>
    </header>

    <section class="sheet-content">
      {{ page.content }}
    </section>
  </article>
</main>

{% include 'site_aside' %}