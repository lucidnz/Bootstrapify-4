<main>
  <div class="sheet">
    <header class="sheet-header">
      <h1>{{ page.title }}</h1>
    </header>

    <section role="main">
      {{ page.content }}
    </section>
    
    
  </div>
</main>

{% include 'aside' %}