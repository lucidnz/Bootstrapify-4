/* Typograhical Styles */

.collection-details,
.product-details {
  @extend .caption;
  @extend .text-center;
}

.collection-title,
.product-title {
  font-weight: $strong-weight;
}

.money-currency {
  @extend .small;
  @extend .text-muted;
}
