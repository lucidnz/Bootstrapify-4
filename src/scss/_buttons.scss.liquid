.btn-contact {
  @extend .btn;
  @extend .btn-default;
}

.product-buttons {
  @extend .btn-group;
  @extend .btn-group-justified;
}

.btn-view {
  @extend .btn;
  @extend .btn-default;
}

.btn-add-to-cart {
  @extend .btn;
  @extend .btn-primary;
  @extend .btn-lg;
  @extend .btn-block;
}

.cart-buttons {
  @extend .btn-group;
}

.btn-update-cart {
  @extend .btn;
  @extend .btn-default;
}

.btn-checkout {
  @extend .btn;
  @extend .btn-primary;
}