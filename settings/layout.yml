Layout:

  Site layout:
    
    Container width:
      name: container_width
      help: "The container holds the width of your content. A fixed width conatiner will stop the content from getting to wide. Full width will allow your content to always flow out to the edge of the screen no matter how big it is."
      type: select
      options:
        fixed: Fixed width
        fluid: Full width
    
  Section layout:
    
    Site header and navigation layout:
      name: header_layout
      help: "Select the site header and navigation layout. You can see each option here [todo: add link]"
      type: select
      options:
        header_centered: 'Centered'